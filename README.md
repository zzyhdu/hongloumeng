红楼梦好啊，红楼梦要看啊。

欢迎同好交流。

## 部署

本项目作为 `sites-stack` 的 `sites/hongloumeng` 子项目部署。生产 Compose、公网 Caddy 和 Docker network 由 `sites-stack` 维护；本仓库只提供站点镜像构建和单项目本地预览配置。

详见 [deploy/README.md](deploy/README.md)。
